<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blog">
	<select id="getBlog" parameterType='string' resultType='blogvo'>
		<![CDATA[
			select * from blog where id = #{id}
		]]>
	</select>
	
	<select id='getCategory' parameterType='string' resultType='categoryvo'>
		<![CDATA[
			select * from category where id = #{id}
		]]>
	</select>
	
	<select id='getPostByCategory' parameterType='integer' resultType='postvo'>
		<![CDATA[
			select * from post where category_no=#{no}
		]]>
	</select>
	
	<select id='getPostByCategoryAndNo' parameterType='map' resultType='postvo'>
		<![CDATA[
			select * from post where category_no= #{categoryNo} and no= #{no}
		]]>
	</select>
</mapper>